node_modules/
dist/
.nx/
coverage/
*.tsbuildinfo
.DS_Store

# .NET build output
bin/
obj/

# Local, personal overrides — not shared
.claude/settings.local.json
.remember/
aiskillsrepokey
.claude/worktrees

# Claude Code local runtime — machine-specific, not shared
# (no trailing slashes: the sandbox mounts some of these as /dev/null char-devices, not dirs)
.claude/agents
.claude/hooks
.claude/routines
.claude/workflows
.claude/launch.json
.claude/scheduled_tasks.json
.claude/loop.md
.claude/output-styles

.mcp.json
.gitmodules

# Editor / IDE
.idea
.vscode

# Stray shell / home dotfiles that land in the repo dir (machine-local, not project files)
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.ripgreprc